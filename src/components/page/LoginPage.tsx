import PageLayout from '../layout/PageLayout';
import LoginFormSection from '../UI/organisms/LoginFormSection';

const LoginPage = () => {
  return (
    <PageLayout pageTitle='Login Page'>
      <LoginFormSection />
    </PageLayout>
  );
};

export default LoginPage;
